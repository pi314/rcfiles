alias grep='grep --color=auto'

alias sudo='sudo -E'

if command -v _pip >/dev/null 2>&1; then
    # Use pip's completion
    compdef pip3=pip
fi

alias -s py=python
alias -s sh=sh

alias wtf='where'
