#compdef sh

_sh () {
    _path_files -/ -g '*.sh'
}

_sh "$@"

# _arguments "*:arguments:(( $(find . -maxdepth 1 -type d | sed 's/\.\///') $(find . -maxdepth 1 -type f | grep '\.sh$' | sed 's/\.\///') ))"
